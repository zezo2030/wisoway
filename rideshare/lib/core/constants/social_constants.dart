/// Social media page links surfaced in the app (Settings → "Follow us").
///
/// NOTE: Replace the placeholder URLs below with the real Facebook / LinkedIn
/// page URLs before release. HTTPS URLs are used so `url_launcher` falls back
/// to the browser when the native app isn't installed.
class SocialConstants {
  SocialConstants._();

  // TODO(team): confirm the official page URLs.
  static const String facebookUrl = 'https://www.facebook.com/visionway';
  static const String linkedInUrl =
      'https://www.linkedin.com/company/visionway';
}
